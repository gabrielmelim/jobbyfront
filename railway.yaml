version: 1
name: my-react-vite-app # Substitua pelo nome do seu app

build:
  env: production
  command: npm install && npm run build

routes:
  - src: /(.*) # Redirecionar todas as requisições para o arquivo index.html
    dest: /index.html